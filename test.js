(function() {
  var width = 1000;
  var height = 500;
  var data = [2, 5, 6, 1, 8, 3, 10]

  var canvas = d3.select("#histogram")
              .append("svg")
              .attr("width", width)
              .attr("height", height);

  var bar = d3.

})();
